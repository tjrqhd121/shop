<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="com.js.mapper.PurchaseMapper">
	<insert id="create">
		insert into purchase(id,del_add,del_name,del_phone) value(#{id},#{del_add},#{del_name},#{del_phone})
	</insert>
	
	<select id="pur_select">
		select 
	</select>		
</mapper>